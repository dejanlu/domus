import React from "react";

const Logo = () => (
  <div className="logo">
    <p className="logo__name">domus</p>
    <span className="logo__subname">nekretnine</span>
  </div>
);

export default Logo;
